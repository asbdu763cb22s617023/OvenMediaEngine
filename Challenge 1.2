# 1.2 leap year def isleap Year(year):

if (year % 4 == 0 and yea r % 100 != 0) or year % 40 0 == == 0:

return True

else:

return False

year = 2012

if isleap Year(year):

print(') is a leap year.'.f

ormat(year))

else:

print('{} is not a leap year..(year)) 
